<% if @vote.valid? %>
  $('#post_<%= @post.slug %>_votes').html('<%= @post.total_votes %> votes');
<% else %>
  alert('You can only vote once per post.');
  //flash message alternative
  //$('<div class="alert alert-error"><a class="close" data-dismiss="alert">x</a><div id="flash_notice">You can only vote once per post.</div></div>').insertBefore('div.span12');
<% end %>